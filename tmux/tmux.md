# tmux
## instal tmp

```bash
git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm
```

- `Prefix + I` for Install
- `Prefix + U` for Update
- `Prefix + alt + u` Remove/uninstall plugins not on the plugin list
